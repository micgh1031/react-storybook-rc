export const typeaheadStyles = {
  defaultStyle: {
    backgroundColor: 'white',
    position: 'absolute',
    top: '100%',
    left: 0,
    minWidth: '100%',
    padding: 'calc(var(--unit) * 1.5) calc(var(--unit) * 2)',
    border: '1px solid var(--aluminum)',
    maxHeight: 'calc(var(--unit) * 16)',
    overflowY: 'auto',
    boxShadow: '0 2px 8px 0 rgba(48,67,81,0.08)',
    borderRadius: '0 0 var(--border-radius) var(--border-radius)',
    transform: 'translateY(-4px)',
    lineHeight: 1.5,
    zIndex: 2,
  }
};
